/** @type {import('next').NextConfig} */
const nextConfig = {
  serverExternalPackages: [
    '@material-ui/core',
    '@material-ui/icons',
    'react-bootstrap',
    'bootstrap',
    'rsuite'
  ]
};

export default nextConfig;
