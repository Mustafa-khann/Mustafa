# Public storefront configuration only. Provider credentials belong on the
# server behind this endpoint and must never be added to the React app.
REACT_APP_COMMERCE_PROVIDER=shopify
REACT_APP_COMMERCE_CHECKOUT_ENDPOINT=/api/commerce/checkout

